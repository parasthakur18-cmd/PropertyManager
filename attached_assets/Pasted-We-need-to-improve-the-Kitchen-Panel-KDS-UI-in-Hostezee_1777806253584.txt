We need to improve the Kitchen Panel (KDS) UI in Hostezee to make it more staff-friendly.

OBJECTIVE:
Add Hindi labels + proper color coding for order statuses across the entire kitchen module.

-----------------------------------
1. STATUS LABEL UPDATE (MANDATORY)
-----------------------------------

Update all order statuses to show both English + Hindi:

- Pending → "Pending (ऑर्डर आया है)"
- Preparing → "Preparing (बन रहा है)"
- Ready → "Ready (तैयार है)"
- Completed → "Completed (दे दिया)"
- Rejected → "Rejected (रद्द)"

Apply this everywhere:
- Top tabs
- Order cards
- Status badges
- Any dropdowns or filters

-----------------------------------
2. COLOR CODING SYSTEM
-----------------------------------

Apply consistent colors:

- Pending → Yellow (#FACC15 or similar)
- Preparing → Orange (#FB923C)
- Ready → Green (#22C55E)
- Completed → Grey (#9CA3AF)
- Rejected → Red (#EF4444)

Ensure:
- Same colors used in tabs + cards + badges
- Background + border + badge all consistent

-----------------------------------
3. ORDER FLOW (IMPORTANT)
-----------------------------------

Maintain this order lifecycle:

Pending → Preparing → Ready → Completed

- When order is created → goes to Pending
- When kitchen accepts → move to Preparing
- When ready → move to Ready
- When served → move to Completed

-----------------------------------
4. UI IMPROVEMENTS
-----------------------------------

- Make status badges more visible (bold + colored)
- Increase font size slightly for Hindi text
- Ensure no text overflow on mobile/tablet
- Keep layout clean and readable

-----------------------------------
5. OPTIONAL (IF EASY)
-----------------------------------

Add sound alerts:

- New order (Pending) → notification sound
- Order ready (Ready) → different sound

-----------------------------------
6. DO NOT BREAK
-----------------------------------

- Existing API structure
- Order status logic
- Database schema

This is only a UI + label + color enhancement.

-----------------------------------

Expected Result:
Kitchen staff should instantly understand order status without needing English knowledge.

-----------------------------------